# Configs

首期固定配置存放位置：

- `templates/`：页面模板和文字区域配置；
- `brands/`：基础品牌规则；
- `qa/`：质检策略和品类规则；
- `recipes/`：内置生成配方种子数据。

当前通过后端固定目录提供以下 P0 配置：

- 5 套页面模板：主视觉居中、左文右图、左图右文、场景叠字、参数信息；
- 1 套已发布基础配方：家电电商详情基础配方；
- 页面类型、文字安全区、候选数量和基础质检策略标识。

配置稳定后迁移为本目录中的版本化 JSON 文件，不改变前端和应用层接口。
