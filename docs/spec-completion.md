# MVP 规格完成说明

## 实现结论

`AI商品图文平台_MVP开发规格.md` 中定义的 P0 功能已实现为可本地运行的端到端系统。P1 是规格中明确的可选增强范围，本期未纳入完成判定；3D、白模、公开 Prompt 市场和专业画布等明确不做项也未实施。

| P0模块 | 已实现内容 | 验证方式 |
|---|---|---|
| 项目与商品资料 | 创建、复制、编辑、结构化事实、参考图分类上传 | API、持久化测试和页面验收 |
| 内容策划 | 五类页面建议；LLM 基于事实、参考图与模板预填标题/正文/视觉目标；逐页/逐字段采用、人工修改、事实审计、降级、版本和确认 | 规划运行 API、字段级采用、数字事实防幻觉、浏览器回归 |
| Prompt与配方 | Prompt版本、变量、配方草稿/发布、已发布限制、项目沉淀 | 配方发布与跨项目流程测试 |
| 图片生产 | 本地可重复生成器和 Azure 生图/编辑适配器，1—3候选；GPT Image 2 预设/自定义尺寸与 Low/Medium/High | 本地端到端测试；真实 Azure 2048 High 单页和五页验收 |
| 版式与版本 | 一个版式库固定一个尺寸；正方形、最大横版和最大竖版库；标题/正文/安全区/商品区可视化拖拽缩放；发布模板修改强制新版本 | 迁移、几何校验、版本不可变、跨库门禁、浏览器比例与拖拽验收 |
| 图文合成 | 模型生成商品与场景底图，系统生成独立文字层和合成图；生成后可调整字体风格、标题/正文字号与颜色、横纵对齐、位置偏移、行距和标题间距，溢出时自动收敛且不重新生图 | 分层文件、底图字节保持、自定义排版与仅重排测试 |
| 智能质检 | 审查计划、文字、参考一致性、视觉变化、遮挡、安全区、事实、品牌、多页样式、评分排序；Azure 模式接入真实 OCR 与多模态审查 | 自动化回归；真实 Azure OCR/LLM 五页验收 |
| 修复闭环 | 文字排版自动修复；一致性问题自动生成修复Prompt并重生成一次；保留修复前后记录 | 专项生产引擎测试 |
| 审核与导出 | 候选对比、问题与位置证据、P0/P1门禁、单页重生成/重排、候选自然语言定向编辑、来源谱系、正式ZIP；多候选选择/排序、纵横拼接、间距/背景/对齐和原分辨率PNG长图导出 | 项目端到端、单图编辑谱系/单页失效、长图像素尺寸与浏览器验收 |
| 多SKU | CSV/XLSX与手工录入、整批配方、独立项目、进度、暂停/继续、失败隔离和重试、按SKU导出 | 多SKU端到端与浏览器验收 |
| 任务与追踪 | SQLite任务表、独立Worker、失败原因、重试、Prompt/模型/配方/质检记录 | 服务重启恢复与导出内容测试 |
| 跨平台运行 | Windows PowerShell 与 macOS/Linux Bash 一条命令同时启停前后端，安全加载 `.env` | Windows 实机启停/健康检查；Bash 脚本与 macOS 路径契约审查 |

## 运行模式

- `PCP_GENERATION_MODE=local`：默认。不依赖外部服务，用于功能演示、自动化测试和稳定回归。
- `PCP_GENERATION_MODE=azure`：通过平台自有适配层调用 Azure 图片生成或参考图编辑。
- `PCP_QA_MODE=local`：默认。使用确定性的文字层、版式、事实和视觉规则证据。
- `PCP_QA_MODE=azure`：调用 Azure AI Vision OCR，并将 OCR、参考图、视觉差异和审查计划交给 Azure OpenAI 多模态审查。
- `PCP_PLANNING_MODE=local|azure`：控制内容规划使用确定性本地方案或 Azure 多模态 LLM；未设置时跟随 `PCP_QA_MODE`。

Azure 活体调用需由运行环境提供服务端凭证。本次验收已使用真实 Azure 图片、Vision OCR 和 LLM 审查资源完成 1 页迭代及最终 5 页 `2048x2048 / High` 全量生产，不再仅依赖适配器模拟。

## 验收记录

- 本轮 AI 规划与候选图定向修改：后端 `82` 项自动化测试全部通过；前端 TypeScript 与 Vite 生产构建通过。应用内浏览器验证了 15→14 项字段级选择、规划建议忽略后的跨页面持久化、候选图定向修改入口、实际 `base.png` 输入说明、质量选择与无相关控制台错误。
- 本轮真实 Azure 调用未执行：当前 `/api/preflight` 与既有任务日志显示 `DefaultAzureCredential` 无法读取本机 `C:\Users\chengbian\.azure\azureProfile.json`（PermissionError），属于本机凭据权限/登录环境阻塞；本地适配器、API 和端到端回归已通过，恢复凭据后可按 `docs/ai-planning-and-candidate-edit.md` 的单页 High 步骤验收。

- 后端：77 项 Python 自动化测试全部通过，覆盖 Azure 路由/身份、版式库迁移、模板版本不可变、跨库门禁、生产、分层合成、自定义排版、长图拼接、QA、配方、批量、导出、服务重启和五页验收项目的干净部署恢复。
- 前端：TypeScript 构建与 Vite 生产构建通过。
- 本地批量：5 个 SKU、25 页完成生产和审核；批量 ZIP 含 5 个独立 SKU 目录；服务重启后仍保持 5/5 完成。
- 配方：黄金案例已沉淀、发布并跨项目复用；当前默认以多张商品/细节参考图直接生成商品与场景，导出清单记录配方、Prompt、模型、实际尺寸/质量、参考列表和图层来源。
- 历史 Azure 五页验收采用 `azure-gpt-image / 2048x2048 / high / layered_product`，最新候选全部 QA 98 分通过并获批准；正式 ZIP 共 31 个条目、78.6 MB。该记录是旧贴层方案的历史证据，不代表当前默认策略；当前默认已迁移到 `model_edit`，并单独执行多参考单图验收。
- 详细证据、项目 ID 和导出文件名见 `docs/acceptance-report.md`。
- 增量浏览器验收：真实五页 Azure 项目成功把第一页改为粗体、104/46 px、标题/正文双色、横纵居中和位置偏移；重排后配置可再次打开读取，底图未重新生成。五页按自定义顺序和 16 px 间距生成 `2048 × 10304` PNG，预览与下载链接均可用，控制台无相关错误。
- 版式中心浏览器验收：`3840×2160` 库与模板按 `16:9` 显示；已发布横版模板创建 V2 草稿后，拖拽标题框会同步更新百分比与实际像素，V1 保持不变。新建项目弹窗展示三种正确比例，竖版示例项目锁定 `2160×3840` 模板，候选原图和显示比例一致。
- 随仓库演示：启动时幂等创建正方形、最大横版和最大竖版三个已完成项目，候选分别为 `2048×2048`、`3840×2160`、`2160×3840`，无需 Azure 即可打开、审核和导出。
- 历史验收恢复：原 `Azure 2048 High Five Page Acceptance` 五页结果已作为仓库资产和幂等种子保存；干净部署可恢复五页规划、5 个 98 分候选、QA、批准状态并导出，不依赖被忽略的本地 SQLite。
