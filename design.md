# Product Content Platform Design Specification

> 版本：1.0
> 状态：后续前端开发的设计基线
> 更新日期：2026-08-12
> 设计方向：B — 雾灰靛蓝 · 轻量创作平台

## 0. 如何使用这份文档

本文档不仅约束视觉风格，也约束信息架构、组件状态、弹层行为、拖拽、缩放、键盘操作、异步任务反馈和异常恢复。新增页面或重构旧页面时，应优先复用本文档已有模式，避免为单个页面创造另一套交互。

规范用词：

- **必须**：交互一致性、数据安全或可访问性的基础要求，不应自行省略。
- **应该**：默认实现；若业务确有原因偏离，需要在代码评审中说明。
- **可以**：增强体验的可选实现。

当本文档与旧页面表现冲突时，以本文档为目标设计；不要求一次性推翻所有旧页面，可按第 22 节的优先级渐进迁移。

## 1. 产品理解与设计目标

本项目不是一个单纯的 AI 图片生成器，而是一个面向电商内容团队的“商品资料 → 内容规划 → 图片生产 → 质检审核 → 交付导出”工作台。它同时包含三类产品形态：

1. **项目管理**：商品档案、素材、内容页、状态与版本。
2. **创作工具**：版式选择、区域编辑、生成候选、局部重做、文字排版和长图拼接。
3. **生产系统**：批量任务、异步进度、质量检查、审核决策、失败重试和结果导出。

主要角色：

- **业务用户**：创建项目、上传素材、规划页面、挑选候选、审核和导出。
- **管理员**：除业务操作外，还负责版式库、提示词/配方、发布状态和系统能力配置。

设计目标：

- 让用户清楚知道自己处于哪一步、下一步能做什么、为何暂时不能做。
- 让创作区域安静、稳定，模板和商品内容成为视觉主角。
- 让 AI 成为普通、可控、可撤销的生产能力，而不是用渐变、发光和“魔法感”包装的主题。
- 长任务不阻断导航；关键审核和不可逆动作有明确防错与恢复路径。
- 同一概念在项目、批量、版式和配置页面中保持相同组件和状态语言。

## 2. 参考边界：借鉴创客贴什么

本方向参考创客贴公开页面中已确认的模式：模板/素材先行、画布居中、属性编辑围绕当前选区、通过点选和拖拽完成创作，以及品牌素材、团队模板、审核和权限等平台化思路。

参考不等于复刻：

- 借鉴“低学习成本的创作工作台”，不复制其品牌、图标、具体布局或装饰。
- 保留本项目更强的电商生产流程、候选比较、QA、批量和版本能力。
- 公开页面无法完整覆盖登录后的深层编辑器状态；本文中关于快捷键、异常处理、冲突恢复和无障碍的细节，是结合本项目业务给出的产品规范。

公开参考：

- [创客贴首页](https://www.chuangkit.com/)
- [创客贴作图流程示例](https://www.chuangkit.com/polymer/961571.html)
- [创客贴企业版](https://www.chuangkit.com/enterprise)
- [创客贴开放平台](https://www.chuangkit.com/apides)

## 3. 设计原则

### 3.1 内容在前，工具退后

大面积使用雾灰工作区和白色内容面；模板、商品图片、候选图负责提供丰富色彩。工具栏、表格和属性面板不使用大面积品牌色。

### 3.2 靛蓝代表操作，不代表装饰

靛蓝仅用于：主按钮、当前选中、焦点环、链接、进度中的关键节点。禁止无意义的紫蓝渐变、发光边框、彩色光斑和“AI 卡片”背景。

### 3.3 流程可见，能力按需出现

项目内始终展示五步工作流；与当前对象有关的属性在右侧检查器中出现，高级参数默认折叠。避免把所有能力堆进一个超长页面。

### 3.4 直接操作必须可精确修正

拖拽和缩放提供即时反馈；右侧数值输入提供精确修改；键盘提供替代操作。所有影响布局的连续拖动只记作一次撤销记录。

### 3.5 状态要说人话

先展示“正在生成第 3/5 页”“图片文字可能有误”等业务文案，再提供可展开的技术详情。不能只显示枚举值、错误码或无限转圈。

### 3.6 风险与确认相匹配

普通切换不确认；可撤销删除不使用强弹窗；会丢失未保存内容、覆盖审核结果或发布配置的动作必须确认。确认按钮要写出动作，例如“删除这 3 页”，不能只写“确定”。

## 4. 信息架构与导航

### 4.1 全局导航

桌面端使用固定左侧导航，建议宽度 `224px`，收起后 `64px`。管理员导航项：

1. 项目
2. 批量任务
3. 版式中心
4. 生成配置

业务用户只显示有权限的入口。导航底部放置系统状态、帮助和用户菜单，不与核心业务入口混排。

规则：

- 当前入口使用浅靛蓝底 + 深色文字 + 左侧 2px 指示条，不使用整块高饱和色。
- 收起状态只显示图标，悬停展示名称；图标按钮必须有可访问名称。
- 浏览器刷新后保留导航收起状态和最近使用的筛选条件。
- 没有权限的入口不展示；暂时不可用但用户有权限的入口展示禁用态和原因。

### 4.2 项目工作区

进入项目后，项目名称、状态、保存状态和主要动作位于页面顶栏；下方使用项目内 Tabs：

`商品资料 → 内容规划 → 图片生产 → 质检审核 → 交付导出`

这五个 Tab 既是导航，也是流程状态提示：

- 当前步骤：深色文字 + 2px 靛蓝下划线。
- 已具备数据：标签旁显示中性计数或完成图标。
- 需关注：显示橙色圆点/计数，不把整个 Tab 涂红。
- 尚不可进入：默认仍可点击查看空状态和前置条件；只有可能造成错误的功能按钮才禁用。
- URL 必须能表达当前项目和 Tab，刷新、返回和分享链接后仍回到相同上下文。

项目状态 `draft / planned / producing / reviewing / completed / archived` 映射为中文“草稿 / 已规划 / 生产中 / 审核中 / 已完成 / 已归档”，状态 Badge 只做提示，不替代流程 Tabs。

### 4.3 页面层级

建议结构：

```text
App Shell
├── 左侧全局导航
└── Main
    ├── 页面/项目顶栏（标题、状态、全局动作）
    ├── 项目流程 Tabs 或页面级工具栏
    └── 内容区
        ├── 列表/导航侧栏（可选）
        ├── 主工作区
        └── 属性检查器（按选区出现）
```

面包屑只在进入深层全屏编辑器时出现，例如“版式中心 / 详情页版式 / v3”，不与项目流程 Tabs 重复。

## 5. 布局模型与响应式

### 5.1 页面容器

- 列表、设置和表单页面：内容最大宽度 `1440px`，水平边距 `24px`（≥1280）、`20px`（1024–1279）、`16px`（<1024）。
- 编辑器和候选审核：允许占满剩余视口，不设置内容最大宽度。
- 页面顶栏高度 `64px`；项目 Tabs 高度 `48px`；常规工具栏高度 `48px`。
- 三栏工作台建议：左栏 `240–288px`，中间弹性宽度，右栏 `300–360px`。
- 侧栏可收起；收起动作不得改变当前选区或画布缩放。

### 5.2 断点

- `≥1440px`：完整三栏，右侧检查器默认展开。
- `1180–1439px`：缩窄两侧栏，次要按钮允许只显示图标。
- `768–1179px`：同一时刻只展开一个侧栏；右侧检查器使用抽屉；表格允许横向滚动。
- `<768px`：支持项目查看、审核、状态和轻量表单；版式拖拽编辑显示只读预览与“请使用更宽屏幕编辑”的说明。不得把桌面编辑器强行等比缩成不可操作的小界面。

所有断点都必须保证：核心动作可达、焦点不丢失、弹窗不超出视口、底部按钮不被软键盘遮挡。

## 6. 视觉系统：雾灰靛蓝

### 6.1 色彩令牌

建议以 CSS 变量实现，业务组件禁止直接写散落的十六进制颜色。

```css
:root {
  --color-bg-app: #f3f4f6;
  --color-bg-subtle: #f8f9fb;
  --color-bg-canvas: #e6e8ed;
  --color-surface: #ffffff;
  --color-surface-raised: #ffffff;

  --color-text: #191c23;
  --color-text-secondary: #555c68;
  --color-text-tertiary: #7a828f;
  --color-text-disabled: #a7adb7;

  --color-border: #dde0e6;
  --color-border-strong: #c9ced7;
  --color-divider: #e8eaee;

  --color-primary-50: #f0f2ff;
  --color-primary-100: #e3e7ff;
  --color-primary-200: #c8ceff;
  --color-primary-300: #a5aef5;
  --color-primary-400: #7e89e4;
  --color-primary-500: #5d69d3;
  --color-primary-600: #4955c2;
  --color-primary-700: #3c46a3;
  --color-primary-800: #313a82;

  --color-success: #2f7d5a;
  --color-success-bg: #edf7f1;
  --color-warning: #94620d;
  --color-warning-bg: #fff7e6;
  --color-danger: #b13c47;
  --color-danger-bg: #fff0f1;
  --color-info: #286b9b;
  --color-info-bg: #edf6fc;

  --color-focus: #6674da;
  --color-overlay: rgb(20 24 32 / 48%);
}
```

使用规则：

- 主按钮默认 `primary-600`，悬停 `primary-700`，按下 `primary-800`。
- 当前行/卡片选择使用 `primary-50` 背景和 `primary-500` 边框，不使用深色反白。
- 普通卡片始终白底；只有浮层、菜单和拖拽对象使用阴影。
- 成功、警告、失败色只用于相应语义，不能用品牌色冒充系统状态。
- 图片上的选框应根据底图自动使用白/深双层描边，不能只依赖靛蓝，以保证任何背景上可见。
- 正文与背景对比度至少 4.5:1；大字和非文本控件至少 3:1。

### 6.2 字体与排版

```css
--font-sans: Inter, "PingFang SC", "Microsoft YaHei", "Noto Sans CJK SC", sans-serif;
--font-mono: "SFMono-Regular", Consolas, "Liberation Mono", monospace;
```

字号层级：

| 用途 | 字号/行高 | 字重 |
| --- | --- | --- |
| 页面标题 | 24/32 | 600 |
| 区块标题 | 18/26 | 600 |
| 卡片标题、弹窗标题 | 16/24 | 600 |
| 正文、表单 | 14/22 | 400 |
| 强调正文、按钮 | 14/20 | 500/600 |
| 辅助文字 | 13/20 | 400 |
| 标签、表头 | 12/18 | 500 |

规则：

- 不使用全大写英文模拟专业感。
- 数字进度、尺寸和时间使用等宽数字 `font-variant-numeric: tabular-nums`。
- 中文标题不超过两级粗细，避免满屏粗体。
- 大段说明最大行宽约 `72ch`，避免横向过长。

### 6.3 间距、圆角、边框与阴影

- 基础网格：`4px`。
- 常用间距：`4 / 8 / 12 / 16 / 20 / 24 / 32 / 40 / 48`。
- 输入框/按钮圆角 `6px`，卡片 `8px`，较大弹窗 `12px`。
- 胶囊圆角只用于 Badge、头像、开关和分段控件；普通按钮不做药丸形。
- 内容分组优先使用留白和 `1px` 分隔线，不为每个小区块套卡片。
- 常规卡片无阴影；菜单 `0 8px 24px rgb(20 24 32 / 12%)`；弹窗 `0 20px 48px rgb(20 24 32 / 20%)`。

### 6.4 图标与插图

- 统一使用一套线性图标，常规 `18px`，工具栏 `16px`，空状态 `40–56px`。
- 图标线宽和视觉面积保持一致；禁止同时混用 emoji、实心彩色图标和多套图标库。
- 只有图标的按钮必须有 Tooltip；危险图标不能只靠红色表达。
- 空状态插图采用低饱和线性图形或产品缩略示意，不使用通用 3D 漂浮机器人和发光星球。

## 7. 动效与层级

### 7.1 动效

- 按压反馈：`80ms`。
- Tooltip/Popover：`120ms`。
- Tab 指示、侧栏展开：`160ms`。
- Dialog/Drawer：`200ms`。
- 最大常规动效不超过 `240ms`。
- 默认缓动：`cubic-bezier(.2, .8, .2, 1)`；退出可更快。

只动画 `transform` 和 `opacity`；高度展开可使用网格行或受控高度，但不能造成大范围跳动。生成进度不使用无意义的循环光效。系统开启“减少动态效果”时，取消位移和缩放，仅保留必要的瞬时显隐。

### 7.2 层级

```text
0    页面内容
10   粘性表头/工具栏
20   拖拽对象、画布控制点
30   下拉菜单/Popover
40   全局 Toast
50   Drawer 遮罩与面板
60   Dialog 遮罩与弹窗
70   全屏编辑器内的确认弹窗
80   关键阻断提示（仅系统级）
```

同一层级不得通过任意大数竞争。嵌套弹窗原则上禁止；需要补充编辑时，在当前弹窗内切换步骤，或使用右侧抽屉。

## 8. 全局交互基础

### 8.1 可点击与焦点

- 桌面端最小点击目标 `32×32px`，主要操作 `40px` 高；触屏最小 `44×44px`。
- Hover 只能增强，不承载唯一信息。
- 所有可交互元素使用 `:focus-visible` 焦点环：2px `--color-focus` + 2px 外间距。
- 禁用控件保留标签可读性，并通过相邻文字或 Tooltip 解释原因。
- 链接执行导航，按钮执行动作；不得用可点击 `div` 代替语义控件。

### 8.2 选择与多选

- 单击卡片/行选中并更新右侧详情；卡片内部按钮不改变选择，除非动作对象就是该卡片。
- `Ctrl/Cmd + 单击` 切换单个多选；`Shift + 单击` 连续范围选择。
- 进入多选后，页面顶栏切换为批量操作栏，并显示“已选 3 项 / 全选本页 / 清除”。
- 批量操作完成后默认保留筛选，清除已失效的选择。

### 8.3 撤销、取消与保存

- `Esc` 依次取消当前拖拽/输入组合、关闭 Popover、关闭非阻断浮层、取消选区；不得一次穿透关闭多层。
- 可编辑器必须支持 `Ctrl/Cmd+Z` 与 `Ctrl/Cmd+Shift+Z`（Windows 兼容 `Ctrl+Y`）。
- 自动保存状态统一显示“正在保存… / 已保存 14:32 / 保存失败，重试”。
- 自动保存建议在停止输入 `800ms` 后触发；拖拽结束后立即触发。保存中的新改动合并到下一次保存，不丢弃。
- 离开存在未保存内容的页面时必须拦截；仅在服务端确认保存后解除。

### 8.4 导航后状态保留

- 返回列表保留页码、筛选、排序和滚动位置。
- 项目内切换 Tab 保留每个 Tab 最近的选中页和滚动位置。
- 刷新页面后恢复服务端已保存内容；临时未提交表单可使用会话草稿恢复，并明确提示“已恢复未提交草稿”。

## 9. 基础组件规范

### 9.1 Button

类型：

- **Primary**：每个视区最多一个最主要动作，如“开始生成”。
- **Secondary**：常规操作，如“保存草稿”“重新检测”。
- **Tertiary/Ghost**：低优先级工具操作。
- **Danger**：确认后的破坏性操作；普通页面不大面积使用红底。
- **Icon Button**：工具栏动作，必须有 Tooltip 和 `aria-label`。

状态必须包含 default / hover / active / focus / disabled / loading。Loading 时保持原宽度，显示微型进度图标并保留动词，例如“正在导出”；防止重复提交，但不要让整个界面失去响应。

### 9.2 表单控件

输入框高度 `40px`，紧凑工具栏可用 `32px`。结构固定为“标签 → 控件 → 帮助/错误”：

- 必填用文字“必填”或星号并提供可访问说明。
- 校验优先在失焦或提交后显示，不在用户输入第一个字符时立刻报错。
- 错误文案说明修复方法，如“请输入 1–200 之间的整数”。
- 数值输入保留单位；上下步进、滚轮修改不得造成误触，聚焦时滚轮默认不改值。
- Select 支持搜索时，输入区必须明确“搜索选项”，无结果时提供清除筛选。
- 长表单底部使用粘性操作栏；顶部同时给出未保存提示。

### 9.3 Tabs 与分段控件

- 页面级 Tabs 使用下划线式，允许横向滚动，不能折成多行。
- 同一数据视图的轻量切换（如“卡片/表格”）使用分段控件。
- 切换 Tab 不弹确认，除非当前 Tab 有未保存表单；此时提供“保存并切换 / 放弃并切换 / 继续编辑”。
- 键盘使用左右方向键切换焦点，`Enter/Space` 激活；激活后焦点管理符合 ARIA Tabs 模式。
- Tab 内容可懒加载，但必须有局部骨架，不能清空整个页面。

### 9.4 Card

- 卡片适合模板、候选图、项目概览；表格数据不要强行卡片化。
- 默认 1px 边框，无阴影；Hover 只提高边框对比，不整体上浮。
- 选中态：浅靛蓝背景 + 靛蓝边框 + 右上选择标记。
- 图片卡片的操作在 Hover/Focus 时出现，但主动作在触屏上始终可达。
- 整卡可点击时，内部按钮必须阻止事件冒泡并拥有独立焦点。

### 9.5 Table

- 表头 `44px`、行高 `52–64px`；首列可粘性，宽表横向滚动。
- 排序只在可排序列显示；点击依次“升序 → 降序 → 默认”。
- 行点击打开详情，操作菜单位于末列；不把 5 个图标按钮平铺在每一行。
- 空值显示“—”，状态使用 Badge，错误信息允许展开查看。
- 大于 50 条使用服务端分页或虚拟列表；加载下一页不清空已显示内容。
- 批量选择框、行操作和行点击不得相互抢事件。

### 9.6 Badge 与状态

- Badge 高度 `22px`，字号 `12px`，浅底深字。
- 中性状态用灰，进行中用靛蓝/信息蓝，成功用绿，需关注用橙，失败用红。
- 不仅使用颜色：同时包含文字或图标。
- 枚举转换必须集中管理，不允许不同页面对同一状态使用不同中文。

### 9.7 Tooltip、Popover 与菜单

- Tooltip 只放短说明和快捷键，悬停/聚焦 `400ms` 后出现，离开延迟 `80ms` 消失。
- Popover 放轻量选择、颜色、筛选；点击触发，点击外部或 `Esc` 关闭。
- Dropdown Menu 使用上下键移动、`Enter` 执行、`Esc` 返回触发按钮；危险项与普通项用分隔线隔开。
- 浮层自动翻转避免溢出，最大高度不超过视口减 `24px`，内容超出内部滚动。
- 点击浮层内部不应意外关闭；提交成功后由动作主动关闭。

### 9.8 空、加载、错误与离线

- **首次空状态**：说明用途 + 一个主动作，例如“还没有项目，创建第一个商品项目”。
- **筛选为空**：显示“没有符合条件的结果” + “清除筛选”，不展示 onboarding 插图。
- **加载**：首屏使用与布局一致的骨架；按钮级操作在按钮内加载；超过 10 秒显示进度说明。
- **局部错误**：错误出现在对应区块，可重试，不抹掉其他成功内容。
- **页面错误**：保留页面框架、项目名和返回路径。
- **离线**：顶栏显示持久提示；允许继续本地编辑时标明“联网后保存”，不允许时禁用提交并解释。

## 10. 弹层系统

### 10.1 何时使用哪一种

| 类型 | 使用场景 | 默认关闭方式 |
| --- | --- | --- |
| Tooltip | 短提示、图标含义、快捷键 | 移开或失焦 |
| Popover | 颜色、筛选、轻量选项 | 点击外部、`Esc` |
| Dropdown Menu | 一组即时命令 | 选择后、点击外部、`Esc` |
| Dialog | 需要集中注意的短任务或确认 | 明确取消、`Esc`、可选遮罩关闭 |
| Drawer | 保留当前上下文的详情/复杂属性 | 关闭按钮、`Esc` |
| Full-screen Editor | 版式、候选比较等沉浸任务 | 返回按钮；未保存时确认 |
| Toast | 已完成、轻量失败、可撤销反馈 | 自动或手动关闭 |

创建项目、编辑商品档案等字段较多的任务，桌面端优先用 `640–720px` Dialog；字段超过一个视口或包含二级导航时改用 Drawer/独立页面，不把弹窗无限加长。

### 10.2 Dialog

- 小确认框宽 `400–440px`；常规表单 `640px`；复杂表单最大 `720px`，均不得超过 `calc(100vw - 32px)`。
- 结构为标题、可选说明、可滚动内容、固定底部操作栏。
- 打开后焦点移到标题或第一个合理控件；焦点锁定在弹窗内；关闭后回到触发元素。
- `Esc` 可关闭无未保存内容的普通弹窗；破坏性确认也允许 `Esc` 取消。
- 表单已有修改时，点击遮罩和关闭按钮不直接丢弃，显示轻量二次确认。
- 默认焦点不能落在危险按钮上；危险确认使用“取消”为次按钮、“删除/覆盖…”为危险按钮。
- 异步提交期间只锁定提交按钮和相关字段；失败保留用户输入，在字段上方显示可重试原因。

### 10.3 Drawer

- 从右侧进入，常规宽 `400px`，复杂检查器 `480–560px`；窄屏占满视口。
- 查看详情的 Drawer 不阻断背景浏览；编辑型 Drawer 打开后背景不可操作。
- Drawer 可在同一位置切换上一项/下一项，避免反复关闭。
- 不在 Drawer 上再叠 Drawer；下一层详情在当前面板内前进，并提供返回。

### 10.4 Toast 与全局通知

- 位置：右上，距离顶栏和右边 `16px`，最大宽 `360px`；最多同时显示 3 条。
- 成功 Toast 4 秒自动关闭；信息 6 秒；错误默认不自动关闭。
- 导出完成等可操作通知包含“下载”按钮；可撤销删除包含“撤销”，保持 8 秒。
- Toast 不能作为错误的唯一载体；对应对象附近必须保留状态。
- 后台任务完成时，即使用户离开原页也可通知；点击通知回到对应项目/任务。

### 10.5 破坏性动作

确认内容必须包含：动作对象、影响范围、是否可恢复。示例：

```text
删除 3 个内容页？
这些页面及其未导出的候选图会被移除。已发布的版式不会受影响。
[取消] [删除 3 页]
```

已归档、可撤销的对象优先“移至归档”并提供撤销；永久删除、覆盖已审核版本、批量重跑等高风险动作才使用强确认。不要让用户手输对象名称，除非影响范围跨多个项目且不可恢复。

## 11. 页面级设计规范

### 11.1 项目列表

页面目标：快速定位项目、了解生产状态、继续下一步。

布局：

- 顶部：页面标题、项目总数、“创建项目”主按钮。
- 第二行：搜索、状态筛选、负责人/更新时间筛选、视图切换。
- 主体默认表格；可选卡片视图面向图片浏览，但筛选条件共用。

表格建议列：项目/商品缩略图、状态、页面进度、需审核、更新时间、负责人、更多操作。点击行进入上次访问的项目 Tab；项目名称旁可显示“下一步：补充素材”等短提示。

交互：

- 搜索输入 `300ms` 防抖，URL 同步查询参数。
- 复制项目先弹出命名和复制范围；完成后打开新项目并标记“复制自…”。
- 归档后从默认列表移除，Toast 提供撤销；归档列表允许恢复。
- 首次空状态直接引导创建；筛选空状态只清除筛选。

### 11.2 创建项目

使用两步 Dialog，减少一次展示过多字段：

1. **基础信息**：项目名、商品名/品类、SKU、目标平台、画面尺寸。
2. **初始资料**：可选上传商品图/详情图/品牌图/场景图，也可跳过后补。

规则：

- 项目名根据商品名自动建议，但允许独立修改。
- 离开第一步后返回不得丢值。
- 上传可在后台继续；项目创建成功后进入“商品资料”，显示仍在处理的文件。
- 同名只警告不强制禁止；明确显示 SKU 或创建时间帮助区分。

### 11.3 商品资料

采用“左侧资料分组 + 中间素材库 + 右侧详情”的结构：

- 左侧：商品档案、商品图、详情图、品牌素材、场景素材及计数。
- 中间：当前分组的网格/列表，顶部有上传、筛选、排序。
- 右侧：选中素材预览、用途、尺寸、文件信息、识别结果和替换/删除。

上传交互：

- 支持点击上传、拖入工作区和粘贴剪贴板图片。
- 拖入时只在合法上传区显示边框，不让整页变成模糊遮罩。
- 文件进入队列后立即显示缩略占位、文件名、单项进度和取消。
- 格式/大小不合法时单项报错，不让一个坏文件阻断其他文件。
- 重复文件提示“疑似重复”，允许保留或跳过，不自动删除。
- 上传成功后可批量修改用途；用途固定为 `product / detail / brand / scene` 对应“商品 / 详情 / 品牌 / 场景”。

商品档案：

- 档案编辑放在右侧 Drawer 或独立面板，按“基础、卖点、规格、品牌约束”分组。
- AI 建议必须显示“当前值 / 建议值”，逐字段勾选应用；禁止一键静默覆盖。
- 应用建议后提供一次撤销，并记录来源和时间。

### 11.4 内容规划

目标是组织 H1–H5、页面类型、文案和版式，不与生成结果混在同一超长页面。

布局：

- 左侧 `260px`：内容页列表，带序号、类型、状态和拖拽手柄。
- 中间：当前页的内容结构、文案字段和模板预览。
- 右侧 `320px`：页面设置、版式选择、AI 规划建议。
- 顶部：新增页面、从模板创建、自动规划、确认规划。

页面类型 `hero / selling_point / function / scene / parameters` 映射为“首屏 / 卖点 / 功能 / 场景 / 参数”。H1–H5 作为结构层级显示，不用不同彩色卡片表达。

交互：

- 新增页插入当前页之后并自动选中；支持从预设页类型开始。
- 删除一页可撤销；批量删除或含已审核候选时二次确认。
- 拖拽只从手柄启动；按下 `Space/Enter` 进入键盘排序，上下键移动，`Space/Enter` 放下，`Esc` 取消。
- 拖动时显示清晰插入线、原位置占位和目标序号；接近容器上下 32px 时自动滚动。
- 自动规划返回建议列表，用户逐页接受、忽略或全选；已手动修改字段默认不被覆盖。
- “确认规划”前展示缺失项；确认后页面进入 `ready`，仍可“取消确认并编辑”，但若已生成需说明影响。

### 11.5 图片生产

布局：

- 左侧：内容页及其生产状态。
- 中间：当前页模板预览、候选图网格/大图。
- 右侧：生成参数摘要、素材引用、任务记录和页级操作。
- 顶部主动作根据状态变化：“生成全部缺失页 / 继续生成 / 重新生成选中页”。

任务规则：

- 点击生成前显示范围摘要（页数、版式、预计消耗，如有），不展示冗长技术参数。
- 任务提交后立即返回任务 ID 和队列态，页面可自由导航。
- 单页状态统一：等待中 / 生成中 / 待审核 / 已完成 / 失败 / 已暂停。
- 进度同时显示总进度和当前动作，例如“3/8 页 · 正在生成卖点页”。
- 失败不抹掉成功页；支持“重试失败项”，默认复用原参数。
- 取消任务要说明已完成结果会保留；取消中的状态不能重复点击。
- 原始错误、模型 ID、请求 ID 放入“技术详情”，支持复制。

候选卡片：

- 图片为主，底部只放候选序号、QA 状态和主操作；更多动作进入菜单。
- 单击选择并在大图查看；双击进入全屏审核。
- “重新生成”默认产生新候选，不覆盖旧候选。
- “图像调整”和“文字重排”进入右侧 Drawer，提交后生成新版本并显示版本来源。
- 候选状态 `generated / needs_review / approved / rejected` 映射为“已生成 / 待审核 / 已通过 / 已驳回”。

### 11.6 质检审核

审核是一个高信息密度工作台：

- 左侧：页面列表和待审核计数。
- 中间：大图、候选切换、对比模式、问题标注叠层。
- 右侧：QA 结果、问题列表、审核意见、通过/驳回。
- 底部可选胶片条：同页候选缩略图。

QA 状态 `pass / review / fail` 映射为“通过 / 需复核 / 不通过”。问题按严重度排序，高风险在前；每条包含位置、规则、原因和建议动作。技术分数放在详情，不让用户从小数猜含义。

交互：

- 单击问题定位并高亮图片区域；再次点击取消定位。
- 可开关“问题标注”“安全区”“文字识别”叠层，叠层开关不写入图片。
- 候选对比支持左右并排和滑杆对比；同步缩放与平移默认开启。
- `A` 通过、`R` 驳回只在焦点不在输入框时生效，并先在界面显示快捷键提示。
- 驳回必须选择原因，可补充备注；通过存在高风险问题的候选时要求填写覆盖理由。
- 审核完成自动前进到下一待审项，但 Toast 提供“返回上一项”。
- 审核决策写入审计记录，展示操作者和时间；更改已通过结果需要二次确认。

### 11.7 交付导出

布局分为“已选页面、拼接预览、导出设置”三部分。导出设置包括：顺序、方向、对齐、间距、背景、输出尺寸和文件格式。

交互：

- 页面选择支持全选、按审核状态筛选；默认只选择已通过页面。
- 顺序拖拽模式与内容规划一致，且提供键盘排序。
- 长图预览使用低分辨率即时更新；最终导出在后台以原始资源执行。
- 设置修改 `300ms` 后刷新预览，旧预览保持到新预览就绪，避免闪白。
- 输出尺寸超限时在设置旁直接说明限制和解决方式。
- 导出任务可关闭面板继续运行；完成后提供下载、复制链接（如支持）和再次导出。
- 导出文件名建议：`项目名_平台_尺寸_日期_版本`，允许提交前编辑。

### 11.8 批量任务

批量任务独立于单项目流程，但复用相同状态和错误组件。

- 顶部提供导入 CSV/XLSX、下载模板、创建批次。
- 导入先预检：总行数、可创建、警告、失败；用户确认后才入队。
- 错误表支持按类型筛选和下载带错误原因的文件。
- 批次详情显示汇总进度和逐项状态；大数据量使用虚拟列表。
- 运行中支持暂停；暂停只阻止新项启动，进行中的项完成后停下。
- 恢复从未完成项继续；重试默认只重试失败项。
- 部分失败 `partial_failed` 显示橙色而非全红，并明确成功/失败数量。
- 导出批次结果不因个别失败被阻断；文件中附失败清单。

状态映射：

| 原始状态 | 中文 | 语义色 |
| --- | --- | --- |
| draft | 草稿 | 灰 |
| ready | 待开始 | 灰/蓝 |
| running | 进行中 | 靛蓝 |
| needs_review | 待审核 | 橙 |
| completed | 已完成 | 绿 |
| partial_failed | 部分失败 | 橙 |
| paused | 已暂停 | 灰 |
| failed | 失败 | 红 |

### 11.9 版式中心

版式中心面向管理员，默认使用卡片网格：缩略图、名称、尺寸、用途、当前版本、发布状态和更新时间。顶部按尺寸、页面类型、状态筛选。

- 发布状态 `draft / testing / published / deprecated` 映射为“草稿 / 测试中 / 已发布 / 已停用”。
- 新建版式先选择画布预设和页面类型，再进入全屏编辑器。
- 复制版式生成新的草稿，不共享后续修改。
- 发布前展示检查结果和影响范围；发布成功保留旧版本，已有项目不被静默替换。
- 停用只阻止新选择，不破坏历史项目。
- 缩略图每次保存布局后异步更新，更新中显示时间而非无限 Loading。

### 11.10 生成配置

提示词、配方和模型配置属于高风险管理能力：

- 列表显示名称、适用页面、版本、状态、最后测试和发布者。
- 编辑使用“基础信息 / 提示内容 / 参数 / 测试记录 / 版本历史”Tabs。
- 变量使用有语义的 Token Chip，悬停显示来源和示例值。
- 发布前必须通过变量完整性检查和至少一次测试；测试结果可与当前发布版并排比较。
- 敏感值只显示掩码；复制和更新敏感值写入审计日志。
- 发布、停用、回滚需要影响摘要；回滚实际创建新版本，不篡改历史。

## 12. 全屏版式编辑器

### 12.1 编辑器外壳

```text
┌ 顶栏：返回 / 名称 / 版本 / 保存状态 / 撤销重做 / 预览 / 发布 ┐
├ 左栏：区域与图层 ┬ 中央：雾灰舞台 + 画布 ┬ 右栏：属性检查器 ┤
└ 底栏：缩放 / 适合视口 / 100% / 坐标提示                    ┘
```

- 顶栏 `56px`；左栏 `240px`；右栏 `320px`；底部缩放控制可浮动在中央舞台左下。
- 中央舞台使用 `--color-bg-canvas`；画布带 1px 边框和轻阴影，画布外不显示多余装饰。
- 默认“适合视口”并留至少 `48px` 舞台边距。
- 画布尺寸来自固定版式规格，屏幕缩放不改变逻辑坐标和导出尺寸。

### 12.2 区域类型与约束

现有区域统一为：标题区、正文区、安全区、商品允许区、商品核心/锚点区。左侧图层显示名称、可见性、锁定状态；必须区域不可删除。

- 标题/正文应该位于安全区内；超出时显示橙色警告。
- 商品核心区必须位于商品允许区内；违反时显示红色错误并阻止发布。
- 宽高最小值由区域类型定义，拖动到最小值后停止，不允许翻转为负尺寸。
- 锁定区域仍可选中查看数值，但不能拖动/缩放；检查器显示“已锁定”。
- 隐藏只影响编辑辅助显示，不改变生成规则，除非产品明确提供“禁用区域”。

### 12.3 选择

- 单击区域选中；`Shift/Ctrl/Cmd + 单击` 多选；空白处单击清除。
- 从空白处拖出选框进行框选；从区域内部拖动始终移动区域，不启动框选。
- `Tab / Shift+Tab` 按图层顺序循环选择；`Esc` 退出多选/清除选择。
- 多选时右侧只显示共同属性；不同值显示“多种值”。
- 选择框和 8 个缩放控制点使用双层描边；控制点视觉 `8px`、命中区至少 `20px`。
- 鼠标移动少于 `3px` 视为点击，达到阈值才开始拖拽，避免轻微抖动。

### 12.4 拖动与吸附

- 拖动时对象直接跟随指针，并在检查器/浮标显示 `x, y`。
- 默认吸附：画布边缘、中心线、安全区边缘和其他区域边缘/中心。
- 吸附阈值以屏幕像素计算，建议 `6px`，不同缩放下手感一致。
- 吸附时显示细靛蓝参考线；离开阈值立即消失。
- 按住 `Alt/Option` 临时关闭吸附；按住 `Shift` 限制为水平或垂直移动。
- 拖至舞台边缘时画布自动平移，速度随距离增加但设上限。
- 超出画布的部分以半透明/警告线表示；合法性在拖动中实时反馈。
- 拖动开始记录初始值，结束后作为一次撤销；按 `Esc` 恢复初始值。

### 12.5 缩放区域

- 拖动四角改变宽高；边中点只改变单轴。
- `Shift` 保持初始宽高比；`Alt/Option` 从中心缩放；两者可组合。
- 检查器提供 `X / Y / W / H` 数值输入和单位 `px`，修改后立即预览，失焦或 Enter 提交为一次历史记录。
- 越界或违反包含关系时，数值框显示错误；不得静默修正成另一个值。可以提供“修正到合法范围”。
- 多选时拖动整体边框按比例缩放；若业务暂不支持，应隐藏多选缩放手柄而不是显示后无响应。

### 12.6 画布缩放和平移

- 缩放范围 `10%–400%`；预设 `25 / 50 / 75 / 100 / 150 / 200%`。
- `Ctrl/Cmd + 鼠标滚轮` 或触控板捏合围绕指针位置缩放。
- 普通滚轮滚动画布舞台；`Shift + 滚轮` 横向滚动。
- 按住 `Space` 后拖动，或使用鼠标中键拖动进行平移；此时光标切换为抓手。
- `+ / -` 以预设步级缩放，`0` 适合视口，`1` 回到 100%。
- 点击“适合视口”重新居中；手动缩放不应自动跳回中心。
- 缩放百分比可直接输入；非法值失焦后恢复最近合法值并说明范围。
- 平移和视图缩放属于查看状态，不进入撤销历史、不触发保存。

### 12.7 键盘精调与快捷键

| 快捷键 | 动作 |
| --- | --- |
| `Arrow` | 选区移动 1px |
| `Shift + Arrow` | 选区移动 10px |
| `Ctrl/Cmd + Z` | 撤销 |
| `Ctrl/Cmd + Shift + Z` / `Ctrl + Y` | 重做 |
| `Ctrl/Cmd + S` | 立即保存 |
| `Tab / Shift+Tab` | 循环选择区域 |
| `Esc` | 取消当前操作/清除选择 |
| `Space + Drag` | 平移舞台 |
| `+ / -` | 放大/缩小 |
| `0` | 适合视口 |
| `1` | 100% |

快捷键只在不与文本输入冲突时生效。首次进入编辑器可展示一次可关闭的快捷键提示；帮助菜单中始终可查看完整列表。

### 12.8 历史、保存与冲突

- 历史至少保存本次会话最近 100 个有效操作；新操作会清空重做栈。
- 连续拖动、连续方向键按住、同一输入框的连续输入分别合并为一个历史步骤。
- 自动保存触发：拖拽结束立即；文本/数字停止 `800ms` 后；发布前强制保存。
- 保存状态位于标题旁，不用 Toast 频繁提示每次成功保存。
- 网络失败时保留本地历史，显示持久的“保存失败”；恢复后自动重试。
- 服务端版本冲突时停止自动覆盖，展示“保留我的版本 / 载入服务器版本 / 另存为副本”；默认推荐另存副本以避免丢失。
- 关闭/返回时若最后一次保存未成功，必须拦截并说明风险。

### 12.9 预览、检查和发布

- 预览隐藏所有辅助线和控件，支持选择测试素材查看真实效果。
- 发布按钮仅在无阻断错误时启用；禁用时点击旁边的“查看 2 个问题”定位错误。
- 检查项包括：区域包含关系、最小尺寸、必需区域、画布规格、名称、测试状态。
- 发布弹窗显示版本说明、变更摘要、适用范围和是否影响新项目；历史项目默认保持原版本。
- 发布后编辑必须产生新草稿版本，不能直接改写已发布版本。

## 13. 图像调整、文字重排与候选版本

### 13.1 图像调整

在候选图右侧 Drawer 打开：上方保留原图缩略和版本信息，下方为“调整说明”和质量/强度等受支持参数。

- 使用自然语言输入时提供任务型示例，如“移除左下角杂物，保持商品不变”，不使用夸张 AI 文案。
- 明确说明会创建新候选，不覆盖当前图。
- 提交后 Drawer 可关闭，任务进入后台；新候选标注“基于候选 2 调整”。
- 历史按时间轴显示输入、参数、结果和操作者，可回到任一版本继续调整。

### 13.2 文字重排

检查器字段：字体、标题/正文字号、对齐、偏移、行距、段间距、颜色自动/手动。字段按“字体 → 布局 → 颜色”分组，高级项折叠。

- 调整数值时使用低分辨率即时预览；停止 `500ms` 后执行完整重排与 QA。
- 自动颜色显示计算结果色块；切换手动时沿用当前计算值作为起点。
- 颜色选择器包含十六进制、最近颜色、品牌色和吸管（如浏览器支持）。
- “应用并重新检测”产生新版本；检测进行中仍保留旧 QA 结果并标注“结果待更新”。
- 重排失败保留输入和旧候选，可重试，不能将画面留成空白。

### 13.3 版本关系

每个候选都显示直接父版本和产生方式（生成 / 图像调整 / 文字重排 / 手动替换）。默认以简洁列表呈现，复杂分支可切换关系图；审核决策只作用于具体版本，不自动继承给子版本。

## 14. 长任务、进度和系统状态

### 14.1 任务生命周期

统一使用 `queued / running / completed / failed`，对用户显示“排队中 / 进行中 / 已完成 / 失败”。业务对象可额外显示“待审核、已暂停”等状态，但底层任务状态不能混用。

任务组件必须包含：

- 动作名称和作用对象。
- 当前阶段与完成数，而非只有百分比。
- 开始时间；超过 30 秒后显示已用时间。
- 可用动作：查看、取消、暂停/恢复、重试、复制错误详情。
- 关闭页面后是否继续运行的说明。

### 14.2 进度真实性

- 有可计数步骤时使用确定进度条；无法估计时使用不确定进度并写明当前阶段。
- 进度不得倒退；服务端阶段权重变化时使用阶段文字而非伪造平滑百分比。
- 100% 仅在结果可用时出现；“处理完成，正在保存”仍显示实际阶段。
- 同一任务不得同时在按钮、页面和 Toast 显示三套不一致进度。

### 14.3 后台任务中心

顶栏提供任务入口和未完成数量，展开后显示最近任务。用户可在任何页面查看进度；完成/失败后保留到已读。系统状态区域只显示服务健康、队列异常等全局问题，不与单个项目失败混在一起。

## 15. 拖拽通用规范

拖拽适用于页面排序、拼接排序、素材导入和画布区域。统一规则：

- 只有明确手柄或明确可拖区域启动排序；输入框、按钮、链接不能启动拖拽。
- 启动阈值鼠标 `3px`、触屏长按约 `180ms`，防止滚动误触。
- 拖拽预览保留对象关键内容，略微提高阴影；原位置保留占位。
- 合法目标显示靛蓝插入线/边框；非法目标显示禁用光标和简短原因。
- 拖入滚动容器边缘自动滚动；拖出应用窗口取消，不删除对象。
- `Esc` 取消并恢复原位；放下后播报新位置，提供屏幕阅读器实时区域。
- 每种拖拽都必须有非拖拽替代：键盘排序、上移/下移按钮或数值输入。

## 16. 图片预览与对比

项目素材、候选图和导出结果复用同一套媒体预览器：

- 单击缩略图选中，点击“放大查看”或双击进入沉浸预览；不要让单击同时承担选择和打开两种冲突动作。
- 预览顶栏显示文件/候选名称、尺寸、版本、下载和关闭；底部提供缩放百分比、适合视口、100% 和上一张/下一张。
- 缩放范围 `10%–400%`，操作方式与版式编辑器一致；`Space + 拖动` 平移。
- 图片小于视口且处于 100% 时不强制放大；“适合视口”可以缩小也可以放大，但最大不超过 100%，除非用户手动放大。
- 切换上一张/下一张时保留“适合视口”模式；手动缩放后是否保留比例作为会话偏好。
- 大图分级加载：先显示可用缩略，再替换高清图；加载失败保留元信息和重试。
- 对比模式支持并排、叠加滑杆；并排默认同步缩放和平移，允许解除同步。
- 标注层与原图分离，导出和下载默认不包含 QA 标注。
- `←/→` 切图，`Esc` 关闭，焦点返回原缩略图。

## 17. AI 建议与自动化能力

AI 不拥有独立视觉语言。自动规划、图像生成、商品档案建议、OCR 和 QA 均使用普通产品组件，只通过明确标签说明能力来源。

### 17.1 建议的基本结构

每条建议至少包含：

- 建议内容。
- 依据或来源摘要（商品档案、素材、规则、模型等）。
- 影响范围。
- 接受、忽略；多字段场景支持逐项选择。
- 生成/检测时间，过期时提示重新运行。

已手动修改的内容不得被后台建议静默覆盖。接受建议后必须可撤销；忽略只影响本次建议，不永久关闭能力。

### 17.2 自动化文案

推荐：

- “根据商品卖点生成页面建议”
- “正在检查图片文字与安全区”
- “建议将标题缩短到 16 字以内”
- “创建新候选，不会覆盖当前版本”

避免：

- “让 AI 施展魔法”
- “一键惊艳全网”
- “智能赋能创作灵感”
- 无法验证的“最佳、完美、100% 准确”

### 17.3 置信度与人工判断

置信度只在能帮助决策时展示，并用“高/中/低 + 解释”表达，不孤立展示小数。QA “通过”表示规则检查通过，不等同于业务审核；最终审核动作必须由有权限用户明确执行。

## 18. 权限、审计与并发

### 18.1 权限表现

- 用户没有某模块的查看权限：导航和入口不展示。
- 用户可查看但不可编辑：显示只读界面，编辑入口隐藏或禁用，并在页顶说明权限来源。
- 因状态暂时不可操作：保留按钮禁用态并说明前置条件，例如“请先确认内容规划”。
- 权限在页面打开期间被撤回：停止提交，保留未保存内容供复制，并提示联系管理员。
- 后端返回 403 时不能只显示“未知错误”，应回到可理解的权限状态。

### 18.2 审计

发布、审核、回滚、批量重跑、覆盖 QA、删除和权限相关动作写入审计记录。展示“谁、何时、对什么、做了什么、结果”，技术详情按需展开。审计记录只读，不提供前端修改入口。

### 18.3 多人并发

- 列表状态可通过轮询或推送更新，但不抢走用户当前选择。
- 正在编辑的表单收到远端更新时显示“此内容有新版本”，不自动覆盖本地输入。
- 版式等强一致对象使用版本号保存；冲突处理见 12.8。
- 审核对象已被他人处理时，提交前再次校验并展示最新决策，允许用户刷新后继续。

## 19. 状态与反馈矩阵

### 19.1 状态中文统一

| 领域 | 原始值 | 用户文案 |
| --- | --- | --- |
| 项目 | draft / planned / producing / reviewing / completed / archived | 草稿 / 已规划 / 生产中 / 审核中 / 已完成 / 已归档 |
| 内容页 | draft / ready | 草稿 / 可生产 |
| 规划运行 | queued / running / completed / failed / dismissed | 排队中 / 规划中 / 已完成 / 失败 / 已忽略 |
| 任务 | queued / running / completed / failed | 排队中 / 进行中 / 已完成 / 失败 |
| 候选 | generated / needs_review / approved / rejected | 已生成 / 待审核 / 已通过 / 已驳回 |
| QA | pass / review / fail | 通过 / 需复核 / 不通过 |
| 发布 | draft / testing / published / deprecated | 草稿 / 测试中 / 已发布 / 已停用 |
| 审核决策 | approved / rejected | 通过 / 驳回 |

新增枚举时，应在集中映射文件和本表同步登记。用户界面不得直接出现原始英文状态。

### 19.2 动作反馈

| 动作类型 | 即时反馈 | 完成反馈 | 失败反馈 |
| --- | --- | --- | --- |
| 轻量保存 | 控件/保存状态显示进行中 | 标题旁“已保存” | 原位置错误 + 重试 |
| 创建/提交 | 按钮 Loading、防重复 | 跳转或 Toast | 保留输入，表单顶部总结 |
| 拖拽排序 | 实时占位和序号 | 自动保存状态 | 回到原顺序 + 错误提示 |
| 后台生成 | 任务进入队列 | 全局通知 + 对象状态 | 对象标红/橙 + 重试 |
| 删除可恢复项 | 立即从视图移除 | Toast“已移至归档” | 恢复原项 + 说明 |
| 导出 | 后台任务卡 | 下载入口 | 保留设置 + 重新导出 |

### 19.3 乐观更新边界

排序、收藏、折叠等易恢复动作可以乐观更新，失败后回滚。审核决定、发布、删除不可恢复对象、生成扣费（如有）和版本回滚必须等待服务端确认，不能先显示成功。

## 20. 无障碍与包容性

目标为 WCAG 2.2 AA，不能等开发结束后再补。

### 20.1 键盘与焦点

- 所有主流程不使用鼠标也能完成；拖拽有键盘替代。
- 焦点顺序与视觉顺序一致；粘性栏和 Drawer 不制造跳跃。
- 打开/关闭弹层、切换 Tab、删除选中项后，焦点移动到可预测位置。
- 不移除系统焦点轮廓；使用统一 `focus-visible` 样式。
- 跳过重复导航，主区域提供“跳到主要内容”。

### 20.2 语义与播报

- 使用原生 button/input/table/dialog 等语义；仅在必要时补 ARIA。
- 图标按钮有可读名称；图片有描述性 alt，纯装饰图 alt 为空。
- 上传、保存、生成进度使用节制的 live region；高频百分比不逐帧播报，可按阶段或每 10% 更新。
- 拖拽排序播报“已将卖点页移动到第 3 位”。
- 表单错误在提交后聚焦错误摘要，并与对应字段关联。

### 20.3 视觉与动作

- 不用颜色作为唯一状态编码；文本、图标或形状同时表达。
- 正文对比度 ≥4.5:1；大文本/控件边界 ≥3:1。
- 浏览器文本放大 200% 后，表单和审核主流程仍可用，不出现内容裁切。
- 支持 `prefers-reduced-motion`；关闭非必要位移、缩放和循环动画。
- 触摸目标 ≥44px；相邻危险与普通操作保留至少 8px 间距。

### 20.4 画布替代操作

画布选区必须能从左侧图层列表聚焦；右侧数值字段可完成移动和尺寸修改；错误关系以文字列出并可点击定位。画布本身难以完整语义化时，必须保证这些结构化替代路径可用。

## 21. 文案与内容风格

语气：简洁、具体、可信，像生产工具而不是营销落地页。

- 按钮使用“动词 + 对象”：保存版式、生成 5 页、重试失败项。
- 标题描述当前对象；说明描述影响，不重复标题。
- 空状态告诉用户为何为空以及下一步。
- 错误先说发生了什么，再给恢复动作，最后才是技术详情。
- 时间采用本地时区和清晰格式；列表可显示“8 分钟前”，悬停显示完整时间。
- 数量始终带单位：“3 个候选”“第 2/8 页”，不孤立展示数字圆点。

示例：

| 场景 | 推荐 | 避免 |
| --- | --- | --- |
| 禁用生成 | 请先确认内容规划 | 暂不可用 |
| 保存失败 | 版式尚未保存，请检查网络后重试 | Error 500 |
| QA 警告 | 标题可能超出安全区 | AI 检测异常 |
| 删除 | 删除这 3 个草稿页面 | 确定 |
| 后台任务 | 已开始生成，可离开此页面 | 任务提交成功 |

## 22. 前端实现基线

### 22.1 Token 优先

颜色、间距、字号、圆角、阴影、动效和层级必须来自统一 Token。组件只引用语义 Token，例如 `surface / border / primary / danger`，不根据页面私自定义“更好看的紫色”。当前版本只设计浅色主题；不要在尚无完整验证时混入半成品深色模式。

### 22.2 组件边界

建议沉淀并复用：

- `AppShell / GlobalNav / PageHeader / ProjectTabs`
- `Button / IconButton / Badge / Tabs / SegmentedControl`
- `Field / NumberField / Select / Checkbox / Radio / Switch`
- `Dialog / Drawer / Popover / DropdownMenu / Tooltip / Toast`
- `DataTable / FilterBar / Pagination / EmptyState / ErrorState / Skeleton`
- `UploadQueue / AsyncTaskCard / SaveStatus / StatusBadge`
- `MediaViewer / CompareViewer / ZoomControls`
- `SortableList / DragHandle`
- `CanvasStage / SelectionBox / AlignmentGuides / Inspector`

组件 API 应以状态和语义命名，避免 `purpleCard`、`magicButton` 这类视觉/营销命名。

### 22.3 现有组件迁移建议

| 现有模块 | 目标位置/模式 |
| --- | --- |
| ProjectTable / ProjectForm | 项目列表 + 两步创建 Dialog |
| AssetPanel / ProfileEditor | 商品资料三栏 + 编辑 Drawer |
| PageEditor / TemplatePreview / LayoutLibraryPicker | 内容规划工作台 |
| ProductionPanel / ProductionProgress / PageJobState | 图片生产 + 后台任务组件 |
| CandidateCard / CandidateEditPanel / TypographyEditor | 候选卡 + 右侧编辑 Drawer |
| StitchComposer | 交付导出工作台 |
| BatchTable / BatchCard / BatchForm | 批量任务列表、详情和导入预检 |
| LayoutCenter / TemplateEditor | 版式中心 + 全屏编辑器 |
| Dialog | 统一弹层系统，补焦点锁定和未保存保护 |

迁移时先统一 Token 与基础组件，再拆项目内五个 Tabs，最后重构全屏编辑器；不要在旧组件内部继续叠加页面专属样式。

### 22.4 性能体验

- 图片网格懒加载缩略图并声明固定宽高，避免加载时布局跳动。
- 大列表使用分页/虚拟化；虚拟列表仍需保证键盘焦点和滚动恢复。
- 高频拖拽/缩放通过局部状态或动画帧更新，避免整个页面每帧重渲染。
- 搜索 `300ms` 防抖，自动保存 `800ms` 防抖；提交、审核等动作不防抖而是明确防重复。
- 页面切换优先保留已加载内容，后台刷新使用局部状态，不闪回整页骨架。
- 图片预览先低清后高清；最终 QA 和导出始终基于原图。

### 22.5 URL 与状态

项目、项目 Tab、当前内容页、审核对象和管理配置应有稳定 URL。筛选、排序和分页适合进入查询参数；临时 Hover、Popover、缩放百分比不进入 URL。浏览器前进/后退必须可预测，不因打开一个 Tooltip 产生历史记录。

## 23. 设计验收清单

每个新增或重构页面上线前逐项检查。

### 23.1 视觉

- [ ] 使用本文 Token，没有页面私有渐变、发光、超大圆角或无意义阴影。
- [ ] 页面只有一个明确主动作；品牌色未被大面积滥用。
- [ ] 标题、正文、辅助信息层级清楚，中文字体和行高一致。
- [ ] 图片、骨架和空状态尺寸稳定，无明显布局跳动。
- [ ] 1280、1440 和窄屏状态下无裁切、重叠和遮挡。

### 23.2 组件状态

- [ ] 按钮/输入/卡片有 default、hover、active、focus、disabled、loading。
- [ ] 列表有首次空、筛选空、加载、局部错误、无权限状态。
- [ ] 禁用操作解释原因；失败保留用户输入和成功结果。
- [ ] 原始状态枚举已映射为统一中文和语义色。

### 23.3 弹层

- [ ] 打开后焦点正确、焦点被约束、关闭后焦点返回。
- [ ] `Esc`、点击外部和关闭按钮行为符合弹层类型。
- [ ] 未保存内容不会因误点遮罩丢失。
- [ ] 弹层不超视口，内容区滚动且底部动作可见。
- [ ] 没有 Drawer 叠 Drawer、Dialog 叠 Dialog。

### 23.4 拖拽与画布

- [ ] 拖动有启动阈值、合法目标、占位、取消和自动滚动。
- [ ] 有键盘或数值输入替代操作。
- [ ] 缩放围绕指针、范围正确，平移和视图缩放不污染撤销历史。
- [ ] 连续拖动只生成一条历史，`Esc` 可恢复初始值。
- [ ] 吸附线、越界和包含关系在不同底图/缩放下均可见。
- [ ] 保存失败、版本冲突和离开未保存页面有恢复路径。

### 23.5 异步与数据安全

- [ ] 任务说明作用对象、当前阶段、进度和可用动作。
- [ ] 用户离开后任务继续，能从任务中心找到。
- [ ] 部分失败不抹掉成功内容，支持只重试失败项。
- [ ] 删除、审核、发布和回滚的确认级别与风险匹配。
- [ ] 重复提交、并发更新和权限变化得到处理。

### 23.6 无障碍

- [ ] 只用键盘可以完成页面主流程，焦点清晰且顺序合理。
- [ ] 图标按钮、表单错误、Tab、Dialog 和状态有正确语义。
- [ ] 颜色不是唯一信息，文本对比度达到 AA。
- [ ] 200% 文本缩放和减少动态效果下仍可用。
- [ ] 上传、保存、排序和后台进度有适度播报。

## 24. 渐进落地顺序

### P0：建立共同语言

1. 落地色彩、字体、间距、圆角、阴影、焦点和动效 Token。
2. 统一 Button、Field、Badge、Tabs、Dialog、Toast、空/加载/错误状态。
3. 建立状态枚举中文映射和异步任务组件。

### P1：重组核心流程

1. 把当前项目长页面拆为五个项目 Tabs。
2. 重构商品资料、内容规划、图片生产和质检审核的信息层级。
3. 将图像调整与文字重排统一为候选版本 Drawer。
4. 完成交付导出与批量任务的失败恢复。

### P2：提升创作效率

1. 重构版式全屏编辑器的选择、拖拽、缩放、吸附、历史和冲突处理。
2. 统一媒体预览、候选对比和 QA 标注层。
3. 补齐键盘排序、画布数值替代操作和全链路无障碍。

### P3：平台化完善

1. 版本历史、审计、多人冲突和后台任务中心。
2. 模板/生成配置的测试、发布、停用和回滚闭环。
3. 基于真实使用数据微调密度、默认值和流程，而非继续添加装饰。

## 25. 非目标与后续决策

当前基线不包含：暗色主题、移动端完整画布编辑、实时多人光标协作、全新品牌 Logo 或营销站视觉。未来若加入这些能力，应单独补充规范，不能用临时样式破坏本设计系统。

仍需在开发过程中用真实数据验证的项目：

- 三栏宽度在超长中文文案和 5K 屏幕下的最佳上限。
- 各画布区域的最小尺寸、包含关系是否需按模板类型差异化。
- 生成阶段能否提供真实可计数进度；无法提供时使用阶段状态。
- QA 严重度与“允许覆盖”的权限规则。
- 导出尺寸、格式、文件大小和批量行数的后端硬限制。

这些待定项只影响业务参数，不改变本文档定义的视觉方向和交互原则。
